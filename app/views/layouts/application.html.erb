<!DOCTYPE html>
<html>
  <head>
    <title>CalorieCounter</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= favicon_link_tag asset_path('favicon.ico')%>
  </head>

  <body>
    <!-- Hero head: will stick at the top -->
  <div class="hero-head">
    <nav class="navbar">
      <div class="container">
        <div class="navbar-brand">
          <a class="navbar-item" href="/">
            CalorieCounter
          </a>
        </div>
        <div id="navbarMenuHeroA" class="navbar-menu">
          <div class="navbar-end">
              <% if logged_in? %>
              <%= link_to "Food Entries", user_food_entries_path(current_user), class: "navbar-item" %>
              <%= link_to "Log out",  sessions_path, data: {turbo_method: :delete }, method: :delete, class: "navbar-item" %>
              <% else %>
              <%= link_to "Sign Up", new_user_path, class: "navbar-item" %>
              <%= link_to "Log in", new_sessions_path, class: "navbar-item" %>
              <% end %>
          </div>
        </div>
      </div>
    </nav>
  </div>
  <% flash.each do |name, msg| -%>
      <%= content_tag :div, msg, class: [name, 'notification'] %>
   <% end -%>

    <%= yield %>
  </body>
</html>
